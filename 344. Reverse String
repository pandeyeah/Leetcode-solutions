class Solution {
public:
    void reverseString(vector<char>& s) {
        int S=s.size();
        int m=0;    
        int n=S-1;
        while(n>m){
            swap(s[m++],s[n--]);
        }
        
    }
};
